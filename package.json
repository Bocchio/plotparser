{
  "name": "plotparser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Digitize numeric data from datasheet plot images, right in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
